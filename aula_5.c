#include <stdio.h>

int main() {

    // operations 

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modules)
    // ++ (increment)
    // -- (decrement)

    // x *= 2
    // x /= 2
    // x += 2
    // x -= 2
    // x %= 2

    int x = 5;
    int z = 3;

    float y = x * (float) z;

    printf("%f",y);


    return 0;
}